<li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newDevoured="{{devoured}}">
		{{#if sleep}}SLEEP TIME!{{else}}Switch to Partner{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">Toss it!</button>
</li>
